<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <!-- Cor de fundo TX -->
    <solid android:color="#0292fd"/> para o tom que preferir -->
    <!-- Contorno preto -->
    <stroke
        android:width="2dp"
        android:color="#000000"/>
</shape>
